To run our project, make sure your terminal is in the right directory of your project, now let's run the development server:

1. To start the development server, you can run `npm run dev` or `yarn dev`

2. Visit http://localhost:3000 to view your application.

3. Edit app/layout.tsx (or pages/index.tsx) file and save it to see the updated result in your browser.
